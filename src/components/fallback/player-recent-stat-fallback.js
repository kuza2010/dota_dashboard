import React from "react";


const PlayerRecentMatchesFallback = () => {
    return (
        <div className="text-center">
            <h2 className="text-warning">
                <br/>
                Sorry, but we <strong>can't load</strong> last player matches. Or something went wrong.
            </h2>
        </div>
    )
};


export default PlayerRecentMatchesFallback;